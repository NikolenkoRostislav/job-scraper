services:
  celery_worker:
    build:
      target: dev
    volumes:
      - .:/app
      - /app/__pycache__

  celery_beat:
    build:
      target: dev
    volumes:
      - .:/app
      - /app/__pycache__
