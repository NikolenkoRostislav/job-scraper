export enum JobOrder {
    UpdateTime = "update_time",
    CreationTime = "creation_time",
    Favorites = "favorites",
}

export enum SeniorityLevel {
    Junior = "junior",
    Mid = "mid",
    Senior = "senior",
}
