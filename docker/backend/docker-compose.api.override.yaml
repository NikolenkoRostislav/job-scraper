services:
  api:
    build:
      target: dev 
    command: uvicorn backend.api.main:app --reload --host 0.0.0.0 --port 8000
    volumes:
      - ../../backend:/app/backend
      - /app/backend/__pycache__
